<% remote_form_for @group do |f| %>
    <%= f.label :title %>
    <%= f.text_field :title %>
  <%= f.submit 'Add group' %>
<% end %>
<%= button_to_remote 'Cancel', :url => {:controller => "groups", :action => "clear_form"} %>